
function degreeConvert(degree) {
    return degree
    
}


try {
    let temperature = "10C"
console.log(`${temperature.toUpperCase()} = ${degreeConvert(temperature)}`);
} catch (error){
    console.log(error);
}